# README Examples

Esta carpeta se mantiene como compatibilidad temporal.

## Ruta canonica actual

Los ejemplos oficiales del proyecto viven en `ejemplos/`.

## Regla

- usar `ejemplos/` para nuevas referencias
- mantener `docs/examples/` solo mientras existan enlaces legacy que dependan de esta ubicacion
