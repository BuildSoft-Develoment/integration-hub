import { Route } from '@angular/router';
import { ConnectionCatalogPageComponent } from './connection-catalog-page';

export const connectionCatalogRoutes: Route[] = [
  {
    path: '',
    component: ConnectionCatalogPageComponent,
  },
];
