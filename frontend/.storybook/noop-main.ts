// Entry placeholder for the Angular browser builder that Storybook references as its
// `browserTarget`. Storybook only reads build options (styles, tsConfig, aot/optimization)
// from that target — it never actually runs this file — so an empty module is enough.
export {};
