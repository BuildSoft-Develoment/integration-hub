# Defectos

## Regla

Registrar aqui defectos abiertos o historicos relevantes cuando se formalicen ciclos de QA.

## Estado actual

- catalogo activo desde 2026-06-12
- **al consultar este fichero, mirar la fecha del ultimo registro**: si no cubre el periodo de los
  cambios que se evaluan, el criterio de salida por defectos no es concluyente

## Registro 2026-08-06 - corrida manual CSRC/CRDR/CCON contra el binario nativo

Ejecucion manual de los casos de catalogo contra `https://app.buildsoft.com.pe/appih` (imagen **nativa**,
stack de integracion on-prem). Evidencia completa en
`qa/fase-6-qa/evidencias/corrida-manual-appih-nativo-20260806.md`.

| ID | Tipo | Severidad | Estado | Descripcion | Evidencia |
| --- | --- | --- | --- | --- | --- |
| QA-PAY-AUTH | Defecto | **Alta** | **VERIFICADO EN SERVIDOR 2026-08-07** (ejecucion #18: money-path COMPLETO en verde, PAY Completada en 1s frente a los 15m31s de fallo previos; imagen `0c382c8f8c05`) | **CAUSA RAIZ CONFIRMADA EN CODIGO + FIX.** `Mt101SftpSinkConnectionResolver.mergedSftpBlock` (:155) copia host/username/**password**/passphrase desde `/sources` con `toMapUnresolved` — correcto y deliberado: el spec persistido del correctivo no debe llevar secretos en claro (ADR-017). PERO ese bloque se inyecta en `Mt101PayTaskProvider.execute` (:200) **DESPUES** de que el motor resolviera la `configuration` de la task, asi que **escapaba a toda resolucion**: nadie expandia la ref ni antes ni despues, y `SftpPaymentTransport` (:143,:189-190) la pasaba literal a `session.setPassword()`. Asimetria que lo delata: el camino CORRECTIVO si resuelve (`dispatchPlanCompiler.materialize(spec, correctiveSecretResolver)`, :430); el NORMAL no tenia equivalente. Y "Probar fuente" funcionaba porque usa `toMap` (resuelto) — dos caminos leyendo el mismo dato con distinta resolucion. **MISMO DEFECTO EN MT101_STATUS** (`Mt101StatusTaskProvider`:744, `withResolvedStatusSink` + `Mt101StatusSftpGateway`:47,69-70), que conecta al mismo banco. **FIX**: metodo `resolveConnectionSecrets(Map)` en el resolver (que ya tiene el `ConfigurationMapper`), invocado por PAY y por STATUS **en el camino EN VIVO unicamente** — el correctivo NO lo llama, para no romper el invariante de refs-congeladas. Se resuelve en memoria y en el ultimo momento: lo persistido siguen siendo referencias. 3 tests nuevos que fijan las dos mitades del contrato (el merge NO resuelve / el resolve SI expande) + suite completa del vertical en verde. | `Mt101SftpSinkConnectionResolver.java:155`, `Mt101PayTaskProvider.java:200`, `Mt101StatusTaskProvider.java:744`, `SftpPaymentTransport.java:143` |
| QA-PAY-AUTH-orig | Nota | - | Superado | (Sintoma original) "La credencial del sink bancario autentica en "Probar fuente" pero FALLA en el despacho de MT101_PAY." Destapado por el arreglo de [QA-PAY-SILENCIO] (ejecucion #17, 07/08 20:56-21:12, imagen `eb47ca528773`): `lastError: "SFTP JSchException: Auth fail for methods 'publickey,password,keyboard-interactive'"` sobre `sendersReference=H1`, `idempotencyKey=/inbox/H1.fin`, `status=INVALIDATED`, `attempts=6`. El sink 7 usa `${secret:tasks/sftp/bank/password}` y esa MISMA referencia **si autentico** contra `sftp-bank` al pulsar "Probar fuente" al crearlo (mensaje "La configuracion de la fuente es valida"). Es decir: la referencia al vault resuelve por el camino de VALIDACION y no por el de DESPACHO. Hipotesis a verificar (sin confirmar): PAY resuelve el secreto por otra via, o congela host/credenciales al compilar el plan de despacho (ADR-017 dice que "el backend resuelve+congela host/credenciales desde la fuente") y en ese punto la referencia no se expande. NO es host key: el error es de metodos de autenticacion, y el sink tiene `Validar host key` en OFF. | `Mt101PayTaskProvider` / compilador del plan de despacho; sink id 7 |
| QA-PAY-SILENCIO | Defecto | **Alta** | **VERIFICADO EN SERVIDOR 2026-08-07** | **CAUSA RAIZ EXACTA + FIX APLICADO 2026-08-07.** `ProcessExecutionService.java:266` llamaba a `failTask(..., auditMapper.buildTaskFailurePayload(taskPlan, executionVariables, triggerSource))`, **descartando `taskPayload`** — la variable con el output de la tarea, en ambito y usada dos lineas mas arriba por `completeTask` (`:279`) y por `completeTaskWithErrors` (`:262`). Es decir: las ramas de exito y de continueOnFailure SI conservaban el output; **la de fallo, que es la que lo necesita, lo tiraba**. Por eso la UI mostraba en la tarea PAY fallida solo `{"taskType","triggerSource","executionVariables"}` (la salida literal de `buildTaskFailurePayload`) mientras la tarea de lectura completada mostraba su output entero. **FIX**: overload `buildTaskFailurePayload(taskPlan, executionVariables, triggerSource, taskOutput)` en `ProcessExecutionAuditMapper` que fusiona el output preservando el plan ante colision (`putIfAbsent`), y se pasa `taskPayload` en el sitio de llamada. Compila y 4 tests nuevos en verde (`ProcessExecutionAuditMapperFailurePayloadTest`). NO hizo falta tocar el modelo de datos: `failTask` ya aceptaba y registraba un `payload` como evento de auditoria. **Requiere rebuild nativo + redespliegue para verificarlo en el servidor.** Historial de diagnostico (3 conclusiones mias erroneas, corregidas al mirar el eslabon siguiente): (1) "no hay traza en ningun sitio" -> falso, el provider si la produce; (2) "se captura y se descarta en memoria" -> falso, se devuelve en el TaskResult; (3) "no hay donde persistirlo, hace falta tocar el modelo" -> falso, el conducto existia sin usar. | `ProcessExecutionService.java:266`, `ProcessExecutionAuditMapper.java:172-215`, `Mt101PayTaskProvider.java:292,939` |
| QA-PAY-SILENCIO-diag | Nota | - | Superado | (Diagnostico intermedio, incompleto) "mal atribuido. El provider SI captura y SI devuelve el motivo; se pierde entre el resultado y la pantalla." Medido: `Mt101PayTaskProvider.java:292` hace `outputs.put("errors", accumulator.errors)` y `:939` llama `addErrorSample(entry)` en la rama misma de `invalidatedCount++`; la tarea NO lanza excepcion, devuelve un TaskResult de fallo CON outputs (`:311-313`). En la UI, para la MISMA ejecucion #15: la tarea "Lectura de archivo" (Completada) muestra en "Payload JSON" el output entero (`recordCount`, `selectedFiles`, `files[]`), mientras que "Pagar MT101" (Fallida) solo muestra la ENTRADA `{"taskType","triggerSource","executionVariables"}` — la clave `errors` no aparece. Es decir: **el detalle de una tarea FALLIDA no expone su output**, justo el caso en que hace falta. Queda por determinar si el motor no persiste outputs cuando el resultado es failure, o si los persiste y el frontend no los pinta. Consecuencia practica igual de mala: 7 iteraciones sin poder saber por que no se pago, teniendo el dato el sistema. `invalidated` = fallo de TRANSPORTE/AUTENTICACION antes del despacho (el banco no recibio nada), segun `:462-471`. Redaccion original (incorrecta) abajo. | `Mt101PayTaskProvider.java:292,311-313,939`; detalle de tarea fallida en #/executions |
| QA-PAY-SILENCIO-orig | Nota | - | Superado | (Diagnostico inicial, incorrecto) "MT101_PAY falla sin dejar NINGUNA traza diagnosticable, ni en la UI ni en el log del servidor." Ejecucion #15 (07/08 18:27:44 -> 18:43:15, 15m31s): la tarea "Pagar MT101" queda **Fallida** y lo unico que se ofrece es el contador `MT101_PAY via SFTP dispatch=1 sent=0 accepted=0 rejected=0 invalidated=1 uncertain=0 retried=1`. En el log del contenedor NO hay excepcion alguna: el runner registra `Background process execution 15 completed for process definition 1` — o sea que la tarea traga el error por completo y ni siquiera propaga al dispatcher. Contraste directo: FILE_DELIVER en la misma sesion si lanzaba `IllegalStateException` con `Caused by: AccessDeniedException: <fichero exacto>`, y eso permitio cerrar aquel caso en un minuto; aqui llevamos 6 intentos sin poder determinar la causa porque **el dato no existe en ninguna parte**. En la tarea que mueve dinero, el operador no tiene forma de saber por que no se pago, ni de distinguir "no salio" de "no se sabe si salio". Agravante: tarda 15m31s (5 reintentos con backoff) en decir siquiera que fallo, y durante ese tiempo no informa de en que intento va. FIX: registrar la excepcion original con stack en cada intento fallido y propagar el motivo a los detalles de la tarea, como minimo lo mismo que ya hace FILE_DELIVER. Sin esto, cualquier incidencia de pago en produccion es indiagnosticable. | `Mt101PayTaskProvider` (camino SFTP), contadores `invalidated` |
| QA-DELIVER-CAUSA | Defecto | **Alta** | **Abierto** | `FileDeliverTaskProvider.java:86-89` captura la excepcion y lanza `IllegalStateException("FILE_DELIVER could not deliver X to sink Y")` **sin encadenar la causa**. El log del contenedor tenia `Caused by: java.nio.file.AccessDeniedException: /work/data/filesystem/qa-filewrite.csv.part` en `FilesystemSink.java:39` — fichero exacto y motivo exacto — y la UI mostro un mensaje inaccionable. Costo 5 iteraciones y 2 hipotesis equivocadas llegar a lo que el backend ya sabia en el primer intento. FIX: pasar la excepcion original como `cause` y propagar su mensaje al detalle de la tarea. Mismo patron que `Cannot inspect SFTP source` (fuentes) y que el `invalidated=1` de MT101_PAY, que tampoco decia que faltase el destino. | `FileDeliverTaskProvider.java:86-89`, `FilesystemSink.java:39` |
| QA-SINK-PROBAR | Defecto | **Alta** | **Abierto** | **El boton "Probar" de una fuente de SALIDA (sink) valida que la ruta EXISTE, no que se pueda ESCRIBIR en ella.** Con `fileNameTemplate` vacio, `FilesystemSourceProvider.selectFiles` (`:46-53`) solo hace `Files.exists(...)` y devuelve la propia ruta; nunca intenta escribir. Reproducido: el sink 8 daba **verde** mientras toda entrega fallaba con AccessDeniedException; cambiar la ruta a una inexistente si daba error, lo que refuerza la falsa sensacion de que "prueba" el destino. En un sink bancario esto es un falso verde que se descubre cuando ya hay pagos fallando. FIX: para direction OUTPUT/BOTH, escribir un temporal y borrarlo, propagando `permission denied` / `read-only file system`. | `FilesystemSourceProvider.java:46-53` |
| QA-FORM-PISADO | Defecto | Media | **Abierto** | En el editor de tareas, **ediciones seguidas en el mismo tick se pisan entre si**: al rellenar los 3 campos "Ancho" de las columnas de detalle de FILE_WRITE de golpe, solo persistio el ULTIMO (`["","","15"]`); hubo que ponerlos de uno en uno con pausa para que quedaran `["30","3","15"]`. Causa: el `draft` del formulario es un `computed` derivado de `task()` y cada `updateDraft` re-emite y recomputa, descartando los cambios previos aun no propagados. Un operador tecleando rapido pierde datos sin aviso. Es el mismo mecanismo de fondo que el bug de `compareWith` de [QA-PAY-SINKREF]. | editor de procesos, patron `draft = computed(() => manager.draftFor(task()))` |
| QA-PAY-SINKREF | Defecto | Media | **Abierto** | **CORREGIDO 2026-08-07 tras verificar en BD: el dato SI se persiste; el defecto es SOLO DE PINTADO.** `SELECT configuration_json FROM process_task_definition WHERE process_definition_id=1 AND task_type='MT101_PAY'` devuelve `"transport":"SFTP","sftp":{"sinkRef":7,...}`. La clasificacion inicial como Critica ("money-path inconfigurable") era ERRONEA: se apoyaba en ver el campo vacio tras F5, observacion que NO distingue "no se guardo" de "se guardo y no se pinta" — con `sinkRef` hidratado a `"7"` (string) y opciones `[value]=7` (number), mat-select sin `compareWith` pinta vacio aunque el dato este. Mismo caso que [QA-DELIVER-SINKREF]. Riesgo residual real: al no reconocer el valor, un reguardado puede revertirlo. El `invalidated=1` de la ejecucion #3 tiene OTRA causa aun sin identificar (candidatos: host key / knownHostsPath del sink 7, o la definicion de tarea con `active=f`). Texto original conservado abajo. | `process-mt101-pay-task-form.component.html:177-184` |
| QA-PAY-SINKREF-orig | Nota | - | Superado | (Diagnostico inicial, incorrecto) "El money-path por SFTP es INCONFIGURABLE desde la UI": el campo "Fuente de salida (destino)" de la tarea MT101_PAY no se puede persistir. Verificado en el nativo con clicks reales: (1) seleccionar el destino NO habilita "Guardar cambios" (el editor no lo marca como modificado); (2) forzando el guardado con otro cambio del mismo formulario, `Extension temporal=.tmp` **si** persiste y `Fuente de salida` vuelve **(VACIO)** tras F5; (3) la ejecucion #3 fallo con `MT101_PAY via SFTP dispatch=1 sent=0 accepted=0 rejected=0 invalidated=1 uncertain=0 retried=1` tras **15m 31s** agotando los 5 reintentos con backoff exponencial. CAUSA PROBABLE (no confirmada por ejecucion, si por lectura): desajuste de tipo en el binding del selector — el modelo declara `sinkRef: string` y se hidrata con `String(sftp['sinkRef'])`, mientras las opciones emiten `[value]="sink.id"` (**number**), y el `mat-select` no declara `compareWith`, asi que compara con `===` y `"7" !== 7`. El provider en si serializa e hidrata bien (`sinkRef: draft.sftp.sinkRef ? Number(...) : undefined`), y `FILE_DELIVER` hace lo mismo sin problema. IMPACTO: no se puede entregar ningun pago por SFTP desde la UI; el fallo tarda 15 min en manifestarse y el mensaje no dice que falte el destino. Deberia ademas fallar **fail-fast** al guardar o al arrancar, no tras agotar reintentos. | `process-mt101-pay-task-form.component.html:177-184`, `mt101-pay-task.provider.ts:164,197,246-252` |
| QA-SINK-MONEY | Defecto | **Alta** | **Abierto** | El flag `moneyCritical` del sink ("Entregar aqui mueve dinero (es un banco)") **nunca llega al servidor**. `source-catalog-command.service.ts:40-46` arma el body con solo 5 claves (name, sourceType, active, configurationJson, direction) y ese mismo objeto alimenta create y update -> no persiste ni creando ni editando. En el servidor `SourceCatalogService.apply()` asigna `definition.moneyCritical` incondicionalmente y `SourceDefinitionRequest` lo declara `boolean` PRIMITIVO, asi que clave ausente y false explicito son indistinguibles: el 200 es honesto, se guardo lo que se pidio. **Consecuencia money-safety**: `MoneyMovementDetector.movesMoney()` decide en OR (por tipo de tarea **o** por `sink.moneyCritical`); MT101_PAY sigue protegido por tipo, pero la rama del sink es hoy **inalcanzable desde la UI**, que es exactamente el caso que el flag venia a cubrir (tarea generica FILE_DELIVER apuntando al banco). Sin el flag, ante lease vencido el motor **re-encola** en vez de enrutar a NEEDS_RECONCILIATION -> riesgo de doble entrega al banco. Agravante: la casilla se deja marcar, devuelve 200 y muestra un texto que **promete** la proteccion. Verificado en el nativo (ID 7 y ID 8, marcado y comprobado checked:true antes de guardar; tras F5 vuelve a false) y en codigo de primera mano. | `source-catalog-command.service.ts:40`, `SourceCatalogService.java:115-126`, `SourceDefinitionRequest.java:13`, `MoneyMovementDetector.movesMoney()` |
| QA-SRC-PICKER | Defecto | Media | **Abierto** | El picker "Definicion de fuente" de la tarea de **lectura** ofrece tambien las fuentes de direccion **Salida (sink)**: en el desplegable aparecian `7 - Sink SFTP Banco QA` y `8 - Sink Filesystem QA`. Filtra por activo (la ID 4 inactiva no sale) pero **no por direccion**, asi que se puede configurar una lectura contra el inbox del banco. El comentario de `process-task.models.ts:45` dice que el picker de FILE_DELIVER si ofrece solo OUTPUT/BOTH; el simetrico de lectura falta. | Observado en el editor de procesos del nativo; `process-task.models.ts:45` |
| QA-CSRC-09 | Defecto | Media | **Abierto** | El alta de fuente **guarda sin la configuracion obligatoria del provider**. Con solo el Nombre relleno el boton *Crear* se habilita aunque `Host` este vacio; ningun campo del provider declara `required`. *Probar fuente* si falla bien (`Missing required configuration key: host`), pero *Crear* persiste igual: quedo la fuente ID 4 "TEMP validacion requeridos" **Activa** con `host` vacio. La validacion de `configSchema()` existe y se aplica en el test, pero **no en el alta/edicion**. Como no hay borrado fisico, la unica limpieza es la baja logica. | `qa/fase-6-qa/evidencias/corrida-manual-appih-nativo-20260806.md` |
| QA-CSRC-11 | Observacion UX | Baja | **Abierto** | En fuentes File system (y FTP/SFTP) el combo `Modo de seleccion` se puede cambiar con la plantilla de nombre vacia, pero el valor **se descarta en silencio**: el frontend solo lo serializa si hay `fileNameTemplate` (`file-system-source.provider.ts:47`) y el backend solo lo usa dentro de `resolveFromDirectory`, al que sin selector no se llega (`FilesystemSourceProvider.selectFiles`). No es perdida de dato -sin selector el valor no tendria efecto- pero el formulario miente. Lo honesto es deshabilitar el combo mientras la plantilla este vacia. | `file-system-source.provider.ts:47`, `FilesystemSourceProvider.java:37-58` |
| QA-CCON-401 | Defecto | Media | **Abierto** | Una escritura rechazada con **401** (token caducado) **no se distingue de una guardada**. `PUT /api/connection-definitions/1` devolvio 401 y el cambio no llego al servidor, pero el editor salio del modo edicion y el detalle renderizo el valor editado como si estuviera persistido; los `GET` seguian dando 200, asi que nada delataba la sesion caducada. Solo al abandonar el formulario aparecio "Tienes cambios sin guardar". Para el operador es indistinguible de un guardado correcto hasta que alguien recarga. Debe forzarse la re-autenticacion y mantener el panel en edicion con el error visible. | `qa/fase-6-qa/evidencias/corrida-manual-appih-nativo-20260806.md` (log de red: PUT 401 y luego PUT 200) |
| QA-CCON-I18N | Observacion UX | Baja | **Abierto** | El error de conexion invalida llega **sin traducir** y sin causa: `Cannot connect using the provided JDBC configuration`, en una consola en espanol. En Fuentes los mensajes equivalentes si estan localizados y son especificos ("La ruta existe pero no hay archivos que coincidan con el selector...", "Missing required configuration key: host"). | `qa/fase-6-qa/evidencias/corrida-manual-appih-nativo-20260806.md` |
| QA-CSRC-06 | Comprobacion | - | Cerrado | **No es defecto, es una comprobacion que faltaba**: tras editar y volver a guardar una fuente cuyo password se muestra enmascarado, *Probar fuente* **vuelve a autenticar** contra el SFTP real. La referencia `${secret:...}` sobrevive el viaje de ida y vuelta por la mascara; el enmascarado de QA-006 no persiste `********` como credencial. | `qa/fase-6-qa/evidencias/corrida-manual-appih-nativo-20260806.md` |

## Registro 2026-07-31 - reingenieria documental y endurecimiento del money-path

Hallazgos de las siete semanas posteriores al registro anterior, consolidados desde los analisis de
`qa/fase-6-qa/` y los mensajes de commit. Los tres primeros son **de producto**, no de documentacion.

| ID | Tipo | Severidad | Estado | Descripcion | Evidencia |
| --- | --- | --- | --- | --- | --- |
| QA-CI-001 | Control ausente | **Alta** | Cerrado | El workflow de CI disparaba sobre las ramas `main` y `develop`, que **no existen** en este repositorio. Los cuatro jobs no se habian ejecutado nunca, asi que todos los gates que las fases 4-7 citaban como control eran ficcion operativa. | `949441a2`; `.github/workflows/ci.yml` |
| QA-CI-002 | Verde falso | **Alta** | Cerrado | El gate de RBAC extraia solo literales entre comillas de `@RolesAllowed(...)`, pero el codigo usa constantes importadas: el conjunto salia vacio, imprimia "Roles en codigo: (ninguno)" y **salia con 0**. Un gate ciego que pasa sostiene una confianza inexistente. | `949441a2`; `ci/scripts/check-rbac-vs-code.mjs` |
| QA-CI-003 | Verde falso | **Alta** | Cerrado | El harvest de `@trace` escaneaba una lista de directorios cableada en cuatro sitios que se quedo en `platform-app`. 82 anotaciones invisibles, 72 de ellas en el vertical del money-path: el gate daba por cubierto el codigo que mueve dinero. | `949441a2`; `ci/scripts/_lib/source-roots.mjs` |
| QA-PAY-001 | Defecto | **Alta** | Cerrado | `RestPaymentTransport` aceptaba un pago cuando la prueba de aceptacion declarada **no estaba presente** en la respuesta 2xx. Cinco causas distintas colapsaban en el mismo `null`, incluido un `successField` mal escrito, que apagaba la comprobacion entera en silencio. | `c81cac3f`, `259b557d` |
| QA-PAY-002 | Defecto | **Alta** | Cerrado | Terminales FALSOS: `{"accepted":null}` y valores no booleanos (`"OK"`, `1`) se resolvian como **rechazo del banco** por `parseBoolean`, con sincronizacion del archivo incluida. Un banco que acusa con "OK" quedaba registrado como rechazo sobre dinero que salio. | `c81cac3f` |
| QA-PAY-003 | Defecto | **Alta** | Cerrado | `SftpPaymentTransport.statRemote` se tragaba cualquier `SftpException` como "el fichero no existe", convirtiendo un permiso denegado en "no hay duplicado, adelante". Y con `posix-rename` sobre OpenSSH, el rename **sobrescribe en silencio**: re-entrega de un pago que el banco pudo haber consumido. | `7901a3cc` |
| QA-PAY-004 | Defecto | Media | Cerrado | Comodin en el `dropPath` resuelto: `?` es caracter legal del set X de SWIFT y el `:20:` solo se valida por longitud, asi que jsch podia redirigir el `put` a un fichero AJENO. | `7901a3cc` |
| QA-AUD-001 | Defecto | Media | Cerrado | La trama por registro del pago aceptado no llevaba la referencia del banco: `audit_record_event.gateway_reference` quedaba vacia para TODO pago aceptado, y la unica prueba vivia en una muestra acotada a 1000. | `9ce95a78`, `V104` |
| QA-AUD-002 | Defecto | Media | Cerrado | La trama `PAY_CONFLICT` del camino STATUS perdia `process_execution_id` y `trace_id`, con lo que el conflicto no aparecia en el drill-down que la consola abre desde la propia fila del conflicto. | `e8b5620d`, `e8b8160e` |
| QA-DOC-001 | Documentacion | **Alta** | Cerrado | Las diez fases contradecian la realidad: 123 desfases (64 de gravedad alta) y 95 huecos. Cuatro documentos ordenaban implementar en `platform-app/`, que rompe el trinquete congelado; uno de ellos es la instruccion que consume un agente de IA. | `d9cf3a8f` y siguientes |
| QA-SEC-001 | Cobertura falsa | **Alta** | **Abierto** | `90.15-seguridad-dependencias` describe una cadena de suministro (gitleaks, syft, trivy, cosign, dependabot) de la que **no existe ni una herramienta**. Marcado como objetivo no implementado; pendiente implementarlo o retirarlo. | `d9cf3a8f` |
| QA-API-001 | Deuda de contrato | Media | **Abierto** | Primera medicion real del gate de API: **123 endpoints en codigo contra 40 en `openapi.yaml`** — 83 sin contrato. No es una regresion: es deuda que llevaba meses sin medirse porque el CI no corria. | `949441a2` |
| QA-CI-004 | Cobertura ausente | **Alta** | Cerrado | `vertical-swift-mt101/pom.xml` no declaraba failsafe: sus tres clases `*IT` se compilaban y **no se ejecutaban nunca**, ni en el CI ni en un `mvn verify` del reactor completo. Entre ellas `Mt101OutboundEndToEndIT`, el E2E del money-path: el modulo que mueve dinero era el unico del reactor sin carril de integracion. | `vertical-swift-mt101/pom.xml` |
| QA-CI-005 | Cobertura ausente | Media | Cerrado | El CI usaba `-pl platform-app -am`; `audit-consumer` es modulo HERMANO, no dependencia, asi que `-am` no lo arrastraba y sus tests -incluido `AuditConsumerKafkaBatchIT`- no corrian. Ambos carriles pasan al reactor completo. | `.github/workflows/ci.yml`, `ci-compat-db.yml` |
| QA-OPS-001 | Riesgo operativo | Media | **Abierto** | Los estados no terminales del money-path (`NEEDS_RECONCILIATION`, `SUSPENDED`) **no son filtrables desde la consola**: hay que consultarlos por base. Documentado en el runbook como limitacion conocida. | `8e4f903a` |

## Registro 2026-06-12 - spec 008 mensajeria de pagos

| ID | Tipo | Severidad | Estado | Descripcion | Evidencia |
| --- | --- | --- | --- | --- | --- |
| QA-008-001 | Brecha de cobertura | Media | Abierto | `payment-rules` simulados quedaron versionados como fixtures y seed SQL local/dev (`bank:SIM-ESTRICTO`, `bank:SIM-FLEXIBLE`). Falta cargar/certificar perfiles reales por banco (`bank:*`) desde guias H2H licenciadas y golden files del banco. | `qa/fase-6-qa/evidencias/008-mensajeria-pagos.md`, `qa/fase-6-qa/perfiles-simulados/` |
| QA-008-002 | Brecha de cobertura | Media | Parcial | REST 4xx/5xx, retries, idempotencia y SFTP con contenedor real quedaron cubiertos; `SKIP_IF_SAME_HASH` valida SHA-256 real. Falta homologar contra gateway bancario real y rechazos semanticos certificados. | `RestPaymentTransportTest`, `SftpPaymentTransportTest` |
| QA-008-003 | Brecha de cobertura | Media | Cerrado tecnico | Se agrego prueba de reproceso selectivo por estado de fragmento: PAY consume `ARCHIVED` por defecto y reprocesa `REJECTED` solo cuando se solicita explicitamente. | `Mt101PayFragmentReprocessTest` |
| QA-008-004 | Riesgo de datos de prueba | Baja | Abierto | La prueba web de reconciliacion compartio base con pruebas masivas previas y mostro `unmatchedSent` alto; se requiere repetir en base limpia para evidencia de cierre. | `#5884` |
| QA-008-005 | Observacion tecnica | Baja | Cerrado tecnico | Nx habia marcado `web:test` como flaky aunque los 56 archivos y 212 tests pasaron; la corrida fresca del 2026-06-21 volvio a pasar sin marca flaky. | `cmd.exe /c "cd frontend && node_modules\\.bin\\nx.cmd run web:test --skip-nx-cache --output-style=stream"` |
| QA-008-006 | Brecha de evidencia UI | Baja | Abierto | Browser verifico login, procesos, modo edicion y paleta `MT101_*`, pero no se capturo el panel de formulario del nodo; queda pendiente evidencia visual de formularios y mapping tipo `DB_WRITE`. | `qa/fase-6-qa/evidencias/008-mensajeria-pagos.md` |
| QA-008-007 | Hardening tecnico | Media | Cerrado tecnico | Se cerro el riesgo P0 pre-homologacion: limpieza de temporales parciales en fuentes FTP/SFTP/REST, SFTP seguro por defecto con `knownHostsPath`, guard de duplicados en V19 sin alterar V18, separacion `dispatchCount`/`sentCount` en `MT101_PAY` y lifecycle MT101 sin fallback legacy. | `TempFileSourcePayloadTest`, `Mt101ArchiveStatusRepositoryTest`, `Mt101PayTaskProviderTest`, `Mt101PayFragmentReprocessTest`, `Mt101AllTasksProcessE2EIT`, `qa/fase-6-qa/evidencias/008-mensajeria-pagos.md` |
| QA-008-008 | Limitacion ambiente | Media | Abierto ambiente | El target `web:build:development` genera el bundle, pero falla al escribir/limpiar `frontend/dist` con `EPERM` del sandbox local (`mkdir`, `open`, `unlink`). `tsc`, `web:lint` y `web:test` pasan; se requiere repetir el build en CI o shell sin restriccion de escritura para cerrar gate release. | `node_modules\\.bin\\nx.cmd run web:build:development --skip-nx-cache --output-style=stream`, `qa/fase-6-qa/evidencias/008-mensajeria-pagos.md` |
| QA-008-009 | Hardening tecnico | Media | Cerrado tecnico | El reproceso/correccion MT101 queda sin fallback por hash+fila: exige `stagingId` y origen, propaga `sourceTaskDefinitionId/sourceName`, lista runs correctivos, sincroniza `PARTIALLY_FAILED` por seleccion, `PAY` correctivo usa claim atomico para evitar doble envio y los fixtures de cuarentena/volumen/providers quedan alineados a V43/V44. | `Mt101CorrectiveLifecycleServiceTest`, `Mt101RebuildServiceTest`, `Mt101StagingCorrectionServiceTest`, `Mt101RowTimelineServiceTest`, `Mt101ReprocessServiceTest`, `Mt101MultiSourceLineageTest`, `Mt101QuarantineServiceTest`, `Mt101LargeVolumeLineageRebuildTest`, `Mt101ArchiveTaskProviderTest`, `Mt101BuildFromTableTaskProviderTest` |
| QA-008-010 | Hardening tecnico | Alta | Cerrado tecnico | El PAY correctivo queda gobernado por hash de payload archivado, claim atomico con lease, invalidacion si cambia el payload, estado `UNCERTAIN` para ejecuciones vencidas y `PARTIALLY_SENT` con detalle por fragmento; la correccion staging exige `If-Match` para evitar sobrescrituras perdidas. | `V44__mt101_corrective_pay_hash_uncertain_partial.sql`, `Mt101CorrectiveLifecycleServiceTest`, `Mt101RebuildServiceTest`, `Mt101StagingCorrectionServiceTest`, `Mt101MillionFileProcessE2EIT`, `mt101-quarantine.component.spec.ts` |
| QA-008-011 | Hardening tecnico | Alta | Cerrado tecnico | Cierre P0 correctivo pre-homologacion sin fallback: `MT101_ROUTE` consume fragmentos persistidos, PAY registra intencion `PREPARED/DISPATCHING` antes del transporte, la correlacion persistida coincide con REST/SFTP, `MT101_STATUS` lee todo el ledger `SENT` paginado y `MT101_RECONCILE` no actualiza archivos ajenos al run correctivo. | `V45__mt101_corrective_route_and_pay_intent.sql`, `Mt101RoutePersistedFragmentTest`, `Mt101PayFragmentReprocessTest`, `Mt101StatusTaskProviderTest`, `Mt101ReconcileTaskProviderTest`, `Mt101CorrectiveLifecycleServiceTest` |
| QA-008-012 | Hardening tecnico | Alta | Cerrado tecnico | `PAY_UNCERTAIN` ya no queda como impasse ni se resuelve con reenvio ciego: se consulta `MT101_STATUS` sobre ledger `UNCERTAIN`, se actualizan fragmentos solo con estados finales y un `PARTIALLY_SENT` puede abrir correctivo hijo solo para fragmentos `REJECTED`, preservando los `SENT`. | `V46__mt101_uncertain_resolution_and_child_corrective.sql`, `Mt101StatusTaskProviderTest`, `Mt101CorrectiveLifecycleServiceTest` |
| QA-008-013 | Limitacion ambiente | Media | Cerrado tecnico | La evidencia visual/a11y autenticada se regenero el 2026-06-26 usando `cmd.exe /c start-platform-stack.cmd`; `http://localhost:8080` y `/q/health` quedaron disponibles, `lh-screenshots.js` autentico y recorrio 9 rutas con 0 errores de consola y 0 botones sin nombre accesible. | `qa/fase-6-qa/evidencias/frontend-visual-a11y-git-2026-06-26.md`, `frontend/scripts/lh-screenshots.js` |
| QA-008-014 | Riesgo de integracion | Media | Abierto | El arbol Git contiene 145 cambios no commiteados distribuidos entre frontend operativo, backend RBAC, docs, tooling Lighthouse y artefactos locales; requiere staging selectivo para evitar mezclar alcances y filtrar archivos con cookies/tokens locales. | `qa/fase-6-qa/evidencias/frontend-visual-a11y-git-2026-06-26.md` |
