# v0.5.0 reingenieria y trazabilidad de features

## Alcance

Instanciacion de la metodologia AI-first por reingenieria sobre el codigo en produccion del
Integration Hub, cerrando la trazabilidad por feature sin tocar codigo funcional. Cubre las
cuatro features reconstruidas y sus requerimientos.

## Features y requerimientos cubiertos

- `001-catalogo-fuentes` — `RF-001`, `RF-002`, `RF-003`, `RF-004`, `RF-005`
  (catalogo de fuentes; endpoints `/api/source-definitions`).
- `002-catalogo-readers` — `RF-001`, `RF-002`, `RF-003`, `RF-004`, `RF-005`
  (catalogo de readers; endpoints `/api/reader-definitions`).
- `003-diseno-y-ejecucion-procesos` — `RF-001`, `RF-002`, `RF-003`, `RF-004`, `RF-005`
  (diseno y ejecucion de procesos; `/api/process-definitions`, `/api/query/process-executions`).
- `004-observabilidad-y-auditoria` — `RF-001`, `RF-002`, `RF-003`, `RF-004`, `RF-005`
  (observabilidad y auditoria; `/api/query/*`).

## Cambios principales

- Modo `origin: reingenieria` por feature: exime la Fase 2 (prototipo/SPDD) para codigo ya
  construido; el resto de la metodologia se mantiene exigible.
- `traceability.md` por feature con matriz RF→API→BD→Codigo→Test real (cero drift).
- `ui-test-cases.md` por feature (casos manuales + cobertura backend existente).
- `contracts/api/openapi.yaml` con las 16 rutas reales (19 operaciones, 5 schemas).
- `tdd-evidence.md` con GREEN real (`mvn -pl platform-app test`: 64 tests, BUILD SUCCESS).
- Runbooks operativos por feature en `ops/runbooks/`.

## Estado de gates

- `gate-deploy-ready`: **pending** — requiere firma humana (no auto-aprobable).
- Las features de reingenieria no requieren `gate-spdd-approved` (Fase 2 N/A).

## Pendiente

- Firma humana de `gate-deploy-ready` para promover Fase 7 a "complete".
- Anotaciones `@trace` en codigo (Fase 5) y cobertura `lcov.info` (Fase 6).
- Corrida dedicada del IT `CatalogAndExecutionResourceIT` para su GREEN.
