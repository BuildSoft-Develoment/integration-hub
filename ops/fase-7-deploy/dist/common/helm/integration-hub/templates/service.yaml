apiVersion: v1
kind: Service
metadata:
  name: {{ include "integration-hub.fullname" . }}
  labels: {{- include "integration-hub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector: {{- include "integration-hub.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
